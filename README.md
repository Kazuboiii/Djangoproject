-- Django commands in git bash--

source (your venv's name)/scripts/activate
pip install django
pip freeze
pip install pillow
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
